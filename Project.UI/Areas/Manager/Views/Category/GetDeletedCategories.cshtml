@model CategoryDto
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Manager/Views/Shared/_Layout.cshtml";
}

<h3 class="mt-4">Pasif Kategoriler Listesi</h3>
<table class="table table-bordered table-striped table-responsive">
    <thead>
        <tr>
            <th>Kategori Adı</th>
            <th>Açıklama</th>
            <th>Aktif Yap</th>

        </tr>
    </thead>
    <tbody>
        @if (Model.Categories.Count() <= 0)
        {
            <tr>
                <td colspan="3">
                    @Model.Message
                </td>
            </tr>
        }
        else
        {
            @foreach (var item in Model.Categories)
            {

                <tr>
                    <td>@item.CategoryName</td>
                    <td>@item.Description</td>
                    <td>
                        <a class="btn btn-danger" asp-area="Manager" asp-controller="Category" asp-action="MakeCategoryActive" asp-route-id="@item.Id">
                            Aktif Yap
                        </a>

                    </td>
                    
                </tr>
            }

        }
    </tbody>


</table>


