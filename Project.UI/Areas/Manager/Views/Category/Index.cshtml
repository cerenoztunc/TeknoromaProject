@model CategoryListDto
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Manager/Views/Shared/_Layout.cshtml";
}

<h3 class="mt-4">Kategori Listesi</h3>
<table class="table table-bordered table-striped table-responsive">
    <thead>
        <tr>
            <th>Kategori Adı</th>
            <th>Açıklama</th>
            
        </tr>
    </thead>
    <tbody>
        @if (Model.Categories.Count() <= 0)
        {
            <tr>
                <td colspan="3">
                    @Model.Message
                </td>
            </tr>
        }
        else
        {
            @foreach (var item in Model.Categories)
            {

                <tr>
                    <td>@item.CategoryName</td>
                    <td>@item.Description</td>
                    <td>
                        <a class="btn btn-danger" asp-area="Manager" asp-controller="Home" asp-action="DeleteUser" asp-route-id="@item.Id">
                            Sil
                        </a>

                    </td>
                    <td>
                        <a class="btn btn-success" asp-area="Manager" asp-controller="Home" asp-action="UpdateUser" asp-route-id="@item.Id">
                            Güncelle
                        </a>
                    </td>
                </tr>
            }

        }
    </tbody>


</table>


